Investigate and fix the following frontend issues in the evade-emergency project. Several may share a root cause (e.g. the dashboard not syncing with real-time state), so diagnose before patching each symptom individually.

Timestamps: Dashboard timestamps differ from the actual device time. Check how timestamps are parsed and rendered — likely a timezone or UTC-vs-local conversion issue. Ensure displayed times match the user's local device time.
Notification count: The notification/badge count keeps increasing even after incidents are resolved. Ensure resolved incidents are excluded from the count and the count updates when an incident's status changes.
Notification sound: Notifications sometimes arrive without playing the alert sound. Ensure the sound reliably plays on each new notification (check autoplay handling and that the audio isn't being blocked or skipped).
Manual refresh required: New notifications only appear after manually refreshing the page. Implement real-time updates (websocket subscription if one exists, otherwise polling) so new notifications and incidents appear without a refresh.
Notification icon visibility: The notification icon should only display on both dashboard an emergencies page